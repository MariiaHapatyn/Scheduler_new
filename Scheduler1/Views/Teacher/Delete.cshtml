@model Scheduler.DTO.Models.TeacherDto

@{
    ViewData["Title"] = "Delete";
}

<div class="container">
    <div class="background m-5 p-5">
        <div class="mb-3">
            <h2>Are you sure you want to delete this?</h2>
            <hr>
            <br>
        </div>
        <div class="form-group">
            <label asp-for="Name" class="control-label"></label>
            <p class="form-control bg-grey text-left"> @Html.DisplayFor(model => model.Name)</p>
        </div>
        <div class="form-group">
            <label asp-for="Name" class="control-label"></label>
            <p class="form-control bg-grey text-left"> @Html.DisplayFor(model => model.Position)</p>
        </div>
        <br>
        <div>
            <a asp-action="Index" class="btn btn-primary float-left">Back to List</a>
            <form asp-action="Delete">
                <input type="hidden" asp-for="TeacherId" />
                <input type="submit" value="Delete" class="btn btn-primary float-right" />
            </form>
        </div>
    </div>
</div>
